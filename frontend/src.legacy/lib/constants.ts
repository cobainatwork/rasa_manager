// ── 分頁 ──────────────────────────────────────────────────────────────────
/** FAQ 列表每頁筆數 */
export const FAQ_PER_PAGE = 20

// ── 編輯鎖 ────────────────────────────────────────────────────────────────
/** 心跳延長鎖的間隔（毫秒）*/
export const LOCK_HEARTBEAT_INTERVAL_MS = 60_000

// ── 輪詢 ──────────────────────────────────────────────────────────────────
/** 同步任務狀態輪詢間隔（毫秒）*/
export const SYNC_POLL_INTERVAL_MS = 8_000

// ── 上傳限制 ──────────────────────────────────────────────────────────────
/** Excel 匯入最大檔案大小（bytes，10 MB）*/
export const IMPORT_MAX_FILE_BYTES = 10 * 1024 * 1024
/** Excel 匯入最大資料列數 */
export const IMPORT_MAX_ROWS = 5_000
