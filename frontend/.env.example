# 前端環境變數範例（複製為 .env 後依環境調整）
#
# VITE_API_BASE_URL：apiClient 的 baseURL。
# - 開發環境：留空字串（預設）即可，所有 endpoint 已內含 /api/v1 前綴，
#   並由 vite.config.ts 的 proxy 將 /api → http://localhost:8000。
# - Docker / 正式環境：若需指向不同 host（例如外部 gateway），可設為完整 URL，
#   例如 https://api.example.com（不要包含結尾斜線）。
# - 預設值（程式內）：空字串
VITE_API_BASE_URL=
